/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add remote hosts here once photos are served from Cloudinary, S3, etc.
    remotePatterns: [],
  },
};

module.exports = nextConfig;
